{% extends 'base.html.twig' %}

{% block title %}Accueil — lazarefortune.com{% endblock %}

{% block body %}
    <main class="mx-auto flex min-h-screen max-w-3xl flex-col justify-center gap-6 px-6 py-16">
        <p class="text-sm font-medium uppercase tracking-wide text-primary-600">V3 clean rebuild</p>
        <h1 class="text-4xl font-bold tracking-tight">lazarefortune.com</h1>
        <p class="text-lg text-slate-600">
            Plateforme vidéo personnelle en reconstruction. Le site public et le Studio seront réimplémentés progressivement.
        </p>
        <div class="flex flex-wrap gap-3">
            <a href="{{ path('studio_home') }}" class="rounded-lg bg-primary-600 px-4 py-2 text-sm font-medium text-white hover:bg-primary-700">
                Ouvrir le Studio
            </a>
        </div>
    </main>
{% endblock %}
