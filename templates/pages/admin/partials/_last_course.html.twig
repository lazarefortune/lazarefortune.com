<div class="mt-16">
    <div>
        <h2 class="h3">Dernières vidéos</h2>
        <p class="text-muted">Les dernières vidéos ajoutées sur la plateforme</p>
    </div>
    <div class="mt-6 grid grid-cols-1 gap-10 sm:grid-cols-2 lg:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4">
        <!-- Simulation d'affichage des vidéos récentes -->
        {% for course in lastCourses %}
            {% include 'pages/public/courses/_course_item.html.twig' %}
        {% else %}
            <p class="text-slate-600 dark:text-slate-400">Aucune vidéo récente à afficher.</p>
        {% endfor %}
    </div>
</div>