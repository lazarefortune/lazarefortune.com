{% extends layout %}

{% import "mails/macros.twig" as element %}

{% block body %}

    {% apply markdown_email %}
        {{ newsletter.content | markdown }}
    {% endapply %}

    {% apply markdown_email %}
        Cordialement,

        L'équipe {{ app_name }}
    {% endapply %}

    {% apply markdown_email %}
        <small>Vous ne souhaitez plus recevoir de newsletter ? [C'est par ici]({{ unsubscribeUrl }})</small>
    {% endapply %}

{% endblock %}
